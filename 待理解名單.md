---
dg-publish: false
---

# 待理解名單

還沒有全面理解、想再花時間啃的東西。每條記「卡在哪」而不只是記「要看什麼」——下次回來才知道從哪切入。

---

## [[0424-Longest-Repeating-Character-Replacement]] — 方法二：窗永不縮小 + `maxcnt` 只增不減

**卡在哪**

`maxcnt` 存的是歷史最佳紀錄、會過期（`s="CCCAB"、k=0` 走到 `r=4` 時窗是 `"CCAB"`，真實 max 只有 2，`maxcnt` 卻停在 3）。過期代表判定會過鬆，為什麼不會放行一個其實不合法的窗？

**已經釐清的**

- 放行**只需要 `maxcnt ≤ 真實max`（不高估）**，不必等於真實 max——因為 `窗長 − 真實max ≤ 窗長 − maxcnt ≤ k`
- 窗卡住（平移）時，判定用的窗長不變（`r` 和 `l` 同時 +1）。上一步 `L − maxcnt > k` 失敗，這一步長度還是 `L`，要翻盤**只剩「`maxcnt` 變大」一條路**，而它變大只可能來自 `++cnt[s[r] - 'A']`，是當前窗的真實計數
- 「從沒卡住過就一路成長」是另一種情形（`k=2、s="ABC"`，`maxcnt` 全程是 1）：此時 `l` 沒動過、計數只增不減，`maxcnt ≤ 真實max` 一樣成立
- 旁證：窮舉字母集 3、長度 1~11、所有 `k`，放行共 2604 萬次，其中 `maxcnt` 過期 **0 次**

**下次做什麼**

- 不看筆記，用自己的話把「為什麼過期值永遠沒機會放行」重講一次
- 搬去 [[1004-Max-Consecutive-Ones-III]]，驗證這個框架是不是真的能原封不動套用

---

## [[0572-Subtree-of-Another-Tree]] — 方法三：序列化 + KMP

**卡在哪**

`prefixFunction` 是照 [[String-Matching]] 抄下來的。知道 `pi[i]` 的定義是「最長真 border」、也知道它能跑，但**沒有自己推過失配時為什麼可以直接跳到 `pi[j-1]` 而不會漏掉解**，目前是「相信它是對的」的狀態，離「白板上憑空重寫」還有一段距離。拼接版的 `pat + '\x01' + text` 也一樣——知道分隔符不能省，但沒真的造出一組省掉就會出錯的輸入。

**已經釐清的**

- 這題會用到 KMP 的理由本身很清楚：序列化之後「子樹」退化成「子字串」，所以 O(n + m) 靠的不是樹的技巧、是字串的技巧
- 序列化的兩個坑已經實測驗證過（null 要留標記、逗號要放在值的**前面**），這部分不用再啃
- 判定寫成 `pi[i] == m` 就是命中，因為分隔符保證 `pi` 不可能超過 `m`

**下次做什麼**

- **在紙上手算一次 `pi`**：自己挑一個短字串（別用 [[String-Matching]] 裡的 `abacabab`，那個表已經有答案了），逐格填。填的時候把「失配 → 退到 `pi[j-1]` → 再比一次」那條回退路徑畫成箭頭，看清楚**為什麼跳過去的那一段一定已經匹配、所以不用重比**
- 手算完再畫一次「樣式在文字上往右滑」的圖，確認回退動的是樣式、`i` 從頭到尾不回頭——這是 O(n) 的來源
- 自己造一組**沒有分隔符就會誤判**的 `pat`／`text`，跑一次驗證分隔符的必要性
- 上面三件事做完，不看筆記重寫 `prefixFunction`，再獨立重寫一次 0572 的方法三

---

## [[0287-Find-the-Duplicate-Number]] — 方法二：值域二分 + 計數

**卡在哪**

還沒細讀，先擱著。真正要自己推出來的是**為什麼可以對「值」二分**——陣列根本沒排序，二分的合法性完全不是來自資料有序，而是來自述詞 `cnt(mid) > mid` 對 `mid` 單調（`cnt(mid)` = 陣列中 `≤ mid` 的元素個數）。這一步沒自己推過之前，看 code 只會停在「它就是這樣寫」。

**已經釐清的**

- 述詞的來源：沒有重複時 `1..mid` 每個值恰好出現一次，所以 `cnt(mid) == mid`；重複值一旦 `≤ mid`，就會把 `cnt(mid)` 頂到 `> mid`
- 它不改動 `nums`，所以跟 Floyd 一樣是**合規解**，跟自己寫的負號標記（方法四）不同——那個違反題目「不准改陣列」的限制
- `hi` 是 `nums.size() - 1` 不是 `nums.size()`：二分的是**值域** `[1, n]`，而陣列長度是 `n + 1`
- 實測在題目上限 `n ≤ 1e5` 時（0.48 ms）跟 Floyd（0.47 ms）完全打平，O(n log n) 多出來的那個 log 在 cache 面前不值錢

**下次做什麼**

- 先回頭做 [[0875-Koko-Eating-Bananas]]（已經到 Box 2），那是「對答案的值域二分」最乾淨的版本，確認框架熟了再回來看 0287
- 不看 code，自己寫出述詞並論證單調性：`mid` 變大時 `cnt(mid) - mid` 只會怎麼動
- 手算 `nums = [3,1,3,4,2]`，列出 `cnt(1..4)`，看單調邊界落在哪（答案：`1, 2, 4, 5`，第一個 `cnt(mid) > mid` 的位置是 `mid = 3`）
- 對照 [[Binary-Search-Templates]] 的「找第一個成立的值」模板，確認 `hi = mid` / `lo = mid + 1` 為什麼不會把答案收掉

---

## [[0287-Find-the-Duplicate-Number]] — 方法三：逐位計數

**卡在哪**

還沒細讀。這招的正確性不像方法二那樣掃一眼就能信——它要證的是一個**嚴格**不等式，而且證明路上有一個很容易算錯的數字：重複值出現 `k` 次時，缺席的值是 `k - 2` 個，**不是 `k - 1` 個**。這個數算錯，推出來的結論會變成「某些 bit 可能漏判」，然後就會誤以為這個解法是錯的。

**已經釐清的**

- 骨架跟方法二同源：都不看陣列順序，只拿「實際計數 vs 理論基準」做判定。差別只在方法二一次判整個值域區間，方法三一次判一個 bit
- `k - 2` 的由來：陣列 `n + 1` 格，重複值佔 `k` 格，剩下 `n + 1 - k` 格放相異值；值域共 `n` 個值，所以缺席 `n - 1 - (n + 1 - k) = k - 2` 個
- 兩邊的不等式（設缺席值中該位為 1 的有 `s` 個，`0 ≤ s ≤ k - 2`）：
    - 答案該位為 0 → `cnt = base - s ≤ base`，不會誤判成 1
    - 答案該位為 1 → `cnt = base + (k - 1) - s ≥ base + (k - 1) - (k - 2) = base + 1 > base`，一定判得出來
- 迴圈裡 `i` 一人分飾兩角（當 index 去取 `nums[i]`、同時當值域裡的數字算 `base`）之所以成立，是因為值域是 `1..n` 而 `i == 0` 對任何 bit 都貢獻 0

**下次做什麼**

- 手算 `nums = [3,1,3,4,2]` 逐 bit 的 `base` 與 `cnt`，親眼看到只有答案 3 的那兩個 bit 會超出基準（答案：`b0` base 2／cnt 3、`b1` base 2／cnt 3、`b2` base 1／cnt 1）
- 再手算一組 `k > 2` 的：`nums = [3,3,3,1,2]`（`k = 3`，缺席 `{4}` 剛好 `k - 2 = 1` 個），確認有缺席值時嚴格不等式還是成立（答案：`b0` 2／4、`b1` 2／4、`b2` 1／0）
- 不看筆記重推一次 `k - 2`，這是整段最容易寫成 `k - 1` 的一步
